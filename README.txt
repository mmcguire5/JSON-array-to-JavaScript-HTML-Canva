

>index.html handles creating a canvas that will display rectangles that are specified in a JSON file
>rectangles.json is the JSON file that contains a dynamic array which holds parameters for rectangles to be displayed on the canvas
>script.js parses the JSON file, sets the attributes of the canvas and displays rectangles on the canvas depending on the parameters held in the JSON file
---------------------------------------------------------------------------------------------------------------------------------------------------------------